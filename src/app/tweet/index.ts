import { types } from "./types";
import { Mutation } from "./mutations";
import { resolvers } from "./resolver";
import { queries } from "./queries";

export const Tweet = { types, Mutation, resolvers, queries }
